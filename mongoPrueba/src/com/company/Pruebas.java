package com.company;

import java.util.HashMap;

public class Pruebas {
    public static void main(String[] args) {
        HashMap<String,Object> cosos = new HashMap<>();

        /*
          String requisitosJson = null;

        try {
            requisitosJson = new ObjectMapper().writeValueAsString(valoresRequeridos);
        } catch (JsonProcessingException e) {
            e.printStackTrace();
        }

        List<Socio> sociosBuscados = new ArrayList<>();

        Document filtroPorEquipo = Document.parse(requisitosJson);

         */
    }
}
